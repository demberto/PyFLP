# Utils

::: pyflp.utils

## Constants


`BYTE` = 0

`WORD` = 64

`DWORD` = 128

`TEXT` = 192

`DATA` = 208

```Python
DATA_TEXT_EVENTS = (
    TEXT + 49,  # Arrangement.name
    TEXT + 39,  # FilterChannel.name
    TEXT + 47,  # Track.name
)
```
